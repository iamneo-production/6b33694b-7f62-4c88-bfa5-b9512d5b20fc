import { RefreeSearchPipe } from './refree-search.pipe';

describe('RefreeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RefreeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
